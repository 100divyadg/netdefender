# netdefender
NetDefender trains three models on the NSL-KDD dataset and measures whether deep learning actually improves on a classical baseline for detecting network attacks. A Random Forest serves as the benchmark; an MLP and a CNN-LSTM hybrid are the challengers. Everything is wrapped in a Flask dashboard that classifies traffic records in real time.
